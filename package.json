{
  "name": "nftagachi-root",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && CI=false npm run build"
  },
  "dependencies": {
    "jpeg-js": "^0.4.4",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "next": "^15.5.12"
  }
}